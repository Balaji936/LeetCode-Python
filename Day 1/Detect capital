class Solution:
    def detectCapitalUse(self, word: str) -> bool:
        if len(word) <= 10:   
             return true
             
        first Letter = word[0] <= z:
        second Letter = word[1] <= z:        
        if not first and second:
            return false
           
        if word[0] and word[1]:
             for i in range(2, n):
           
           
    
